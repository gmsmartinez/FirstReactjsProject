import React from "react";

function Title(props) {
    const { title } = props;
    return <h3>{title}</h3>;
    
    //return <h3>Soy un titulo</h3>;

    //es este return no puse los parentesis porque solo tengo una etiqueta
}

export default Title;